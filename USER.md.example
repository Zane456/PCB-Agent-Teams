# 使用者档案（模板）

> 复制成 `USER.md` 再填：`cp USER.md.example USER.md`。
> `USER.md` 是你的私人档案，已被 `.gitignore` 排除，不会进 git。
>
> 跨项目长期事实：在手资源、研究方向、沟通偏好。
> 任何 skill 在做选型/讨论前都应**先读本文件**，不要假设用户有什么。
> 已知项保持更新，未知项标 `[待填]` —— 由 user 在第一次相关讨论时补上。

---

## 0. 基本信息

| 项 | 值 | 影响 |
|---|---|---|
| **所属地（高优先级因子）** | **日本** | 决定 `component-selecting-<locale>` 路由。已实现：`日本`（component-selecting-JP，需免费 DigiKey key）/ `中国大陆`（component-selecting-CN，零 key）；其他地区先读工作区 CLAUDE.md §"Locale 路由" |
| 学术身份 | `[待填]` | 实验室设备配置、安全规程、采购流程 |
| 时区 | `[待填]`（如 JST (UTC+9)） | 邮件 / 会议讨论时间 |
| 沟通语言 | `[待填]` | 见 §5 |

---

## 1. 在手硬件

| 类别 | 型号 / 描述 | 备注 |
|---|---|---|
| MCU 开发板 | `[待填]` | 控制平台 |
| 板载调试器 | `[待填]` | |
| 直流电源 | `[待填]`（型号 / 最大电压 / 最大电流） | 决定桌面能跑多高电压 |
| 电子负载 | `[待填]` | |
| 示波器 | `[待填]`（型号 / 带宽 / 通道数 / 是否带差分探头） | 影响 EMC / 高速信号测试能力 |
| 万用表 | `[待填]` | |
| 隔离变压器 | `[待填]`（有 / 无） | 决定能否安全测试非隔离原型 |
| 高压源 | `[待填]`（最大输出 V / 限流） | 高压主功率测试需要 |
| LCR 表 | `[待填]` | |

---

## 2. 在手元件库存

由 `component-preparing` skill 维护，或 user 手动列。

- 散件库存：`[待填]`
- 已购但未上板的模组：`[待填]`

---

## 3. 测试环境

| 项 | 状态 |
|---|---|
| 物理位置 | `[待填]`（宿舍 / 学校实验室 / 共用工位）|
| HV 工作能力 | `[待填]`（最高安全工作电压、是否有隔离工位） |
| 焊接能力 | `[待填]`（手工烙铁 / 热风 / 回流；最小封装：被动件 0603? 0402?；IC 最小 pitch？能不能吃 QFN / BGA；功能等价时是否优先通孔） |
| 返修能力 | `[待填]`（拆 IC / 补焊飞线？） |
| 噪声 / 空间限制 | `[待填]` |

---

## 4. 长期研究方向

- **大方向**：`[待填]`
- **学术身份**：`[待填]`
- **当前子模块清单**：
  - `[待填]` — 命名用「功能_参数」格式（如 `buck_5v_3a`）

---

## 5. 沟通偏好

- **语言**：`[待填]`
- **风格**：`[待填]`（如：简洁分步、讲原理、不废话、不无脑附和）
- **PCB 经验**：`[待填]`（零 / 入门 / 熟练 —— 决定要不要从头讲概念）
- **数学表达**：`[待填]`（如 LaTeX `$...$`，或纯文本）
- **不要做**：`[待填]`（如：跳步骤直接给"现成模块"、用"业界一般这样做"敷衍）

---

## 6. 未决问题（user 自己也没答案的）

- `[待填]` — 以列表形式记录长期悬而未决的方向性问题（不是项目级 TODO）

---

## 维护规则

- skill 在 Phase 1 / Phase 2 讨论时若发现 `[待填]` 项**正好被本次需求触及**，应主动问用户并把答案回写本文件
- 不要把项目专属内容（具体 BOM、电路细节）写进来 —— 那些去 `Projects/<name>/CLAUDE.md`
- 不要把工程铁律写进来（电源域电压、HV/LV 地分离等）—— 那些在 SKILL.md
